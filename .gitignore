# misc-api 忽略规则

# 配置文件（不提交敏感配置）
config/database.php
config/install.lock
config/.security/

# 运行时数据
data/*
!data/.gitkeep
!data/.htaccess
!data/playground/
!data/playground/.htaccess
!data/playground/.gitkeep

# IDE / 编辑器（本地）
.idea/
.vscode/
.cursor/
*.swp
*.swo
*~

# 本地 Git 钩子（仅维护者本机；禁止进仓库与发行 ZIP）
.githooks/

# 系统文件
.DS_Store
Thumbs.db

# 临时文件
*.log
*.tmp

# 发行包（本地打包产物）
release/
release/*.zip

# 禁止提交的本地打包工具（发版用临时 PHP ZipArchive，用完即删；勿再建 tools/）
/tools/
/pack-release.ps1

# 本地参考文档（其它项目 README 范本，勿提交）
README - 副本.md

# 本地开发规范文档（整目录不提交、勿打进发行 ZIP）
开发规范/

# 本地修改流程记录（按版本对话摘要，不提交、勿打进发行 ZIP）
修改流程记录/

# 发行说明（v13.10.0 起仅本机可选；禁止进仓库与发行 ZIP，E174）
发行说明/

# 本地运维参考
svg图标.txt

# 注意：nginx伪静态配置.md 须随仓库/ZIP 发布，勿再忽略

# 参考对照目录（本地其它项目 / 主题参考 UI / 后台大屏参考，勿提交、勿打进发行 ZIP）
# 强制：凡根目录名含「参考」的目录、以及下列具名目录，均不得进入 Git 与 apinexus*.zip
参考api系统/
极验验证码/
参考*/
主题参考/
主题一UI参考设计/
默认主题参考UI*/
默认主题参考UI（主题一）/
管理员后台参考设计/
管理员后台数据大屏参考UI/
管理员后台数据大屏参考UI/**
*参考UI*/
*参考UI*/**

