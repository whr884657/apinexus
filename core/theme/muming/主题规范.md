# ApiNexus · 主题六规范（muming）

> **这份规范随主题包走。** 目录：`core/theme/muming/`。自定义、改版这个主题时以本文为准。  
> 维护者在本地还有一份同文：`开发规范/主题六规范.md`（不进安装包）。两边改动必须一起改。  
> 对外名「主题6」。开发者：慕名。定位：七牛云式浅色企业蓝。  
> 原主题五 `fifth` 已下线；曾启用 `fifth` 的站点请改选本主题。

---

## 一、这是谁

浅色企业蓝 SaaS。顶栏是白底吸顶：logo、居中链接、登录按钮、主题切换。  
class 前缀用本包已有的 `th5-*`，不要改去用 `st-*` 或 `th3-*`。  
资源只出 `core/theme/muming/assets/`。禁止把其它主题的页面 HTML / CSS 整段搬进来改个色。  
预览图与主题内配图由维护者完善，**禁止**擅自替换主题包内图片。

数据走 `Frontend*`。详情、在线测试、OpenAPI、复制按钮的能力要齐，样子自己做。  
后台自定义设置：`lib/admin-settings.php` → `vs_theme_admin_render_settings_muming`。

---

## 二、首页公告

没有已发布公告，或关掉 `show_home_announce`：不输出滚动条、弹窗和脚本。禁止写死欢迎占位公告。  
弹窗打开前挂 `document.body`。禁止整卡贴顶，禁止手机正文 `max-height: none`。

---

## 三、登录与顶栏

未登录「登录 / 注册」，已登录「控制台」。登录字段顺序见《登录认证表单顺序规范》。

---

## 四、改这个主题时

- [ ] 只改 `core/theme/muming/`
- [ ] 没有从其它主题复制页面结构
- [ ] 本文件与 `开发规范/主题六规范.md` 一起改
- [ ] 未擅自替换主题包内图片
