---
description: 禁止 Git 提交带任何 AI/Cursor 协助署名
alwaysApply: true
---

# Git 提交禁止 AI / Cursor 署名（强制）

## 铁律

1. **任何** `git commit` / `git commit --amend` / 生成的提交说明中，**禁止**出现：
   - `Co-authored-by: Cursor`
   - `Co-authored-by: …cursoragent@…` / `@cursor.com`
   - `Made with Cursor` / `Made-with: Cursor`
   - 其它表明「AI 协助 / AI 共同作者 / generated by AI」的 trailer 或脚注
2. 本仓库代码与提交的版权与作者归属为维护者本人；**不得**把 Cursor 或任何 AI 产品标为共同作者。
3. Agent 执行提交时：**不得**主动添加 `--trailer`；若环境自动注入 trailer，须依赖仓库 `.githooks/prepare-commit-msg` 剥离；提交后用 `git log -1 --format=%B` 确认正文与 trailer 区均无上述字样。
4. 若发现历史未推送提交带有上述署名，发版前须清理后再 push（改写仅限未推送范围，禁止擅自改写已上远程的历史，除非维护者明确要求）。

## 提交信息只允许

- 业务说明（feat/fix/docs 等）与版本号
- 变更摘要

**不允许**任何 AI 工具品牌、协助声明、生成声明。
