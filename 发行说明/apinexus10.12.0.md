# ApiNexus 10.12.0

## 本版要点

- 新增 **`statday`** 按日聚合表（滚动固定 30 天）
- 每次调用三写：`api.calls` + 日表增量 +（详情开时）`apilog`
- 控制台今日调用 / 成功失败 / 类型与成功率趋势 / TOP **优先读日表**
- 升级自动回填近 30 天历史空洞
- 关于页主题变量 IDE 告警修复

## 升级注意

- **有数据库结构变更**：执行迁移 `install/migrations/10.12.0.sql`（在线更新会自动跑）
- 强刷控制台相关 JS/CSS 缓存即可
