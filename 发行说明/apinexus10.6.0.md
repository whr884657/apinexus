# ApiNexus 10.6.0

## 本版要点

- **接口列表 QPM 标签**：管理端接口列表与用户开发者 API 管理卡片，在 `qpm>0` 时显示 `QPM N/MIN`；为 0 不显示
- **积分变动大规模搜索**：先解析用户 ID / 类型 `(direct,kind)` / 单号，再精确过滤；禁止对大表拼多路 `%LIKE%` OR（超时后整页「搜不到」）
- **索引**：`orders.idx_remark_prefix`（备注前缀兜底）；迁移 `install/migrations/10.6.0.sql`
- **Redis 业务缓存**：监控列表覆盖更多逻辑键；展示键名不写中文用途说明；搜索框放大镜对齐

## 升级注意

- 请在后台执行**数据库结构更新**（应用 `10.6.0` 迁移）
- 覆盖代码后强刷后台 / 用户中心静态资源，核对接口列表 QPM 标签与积分搜索
